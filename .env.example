# Database Connection (SQLite local file)
DATABASE_URL="file:./dev.db"

# Meta WhatsApp Cloud API Secrets (Server Side Only - Never exposed to client)
META_WEBHOOK_VERIFY_TOKEN="clinic_ai_secret_verify_token_2026"
META_APP_SECRET="mock_meta_app_secret_998877"

# Google Calendar Integration (Optional mock / API keys)
GOOGLE_CALENDAR_CLIENT_ID="mock_google_client_id.apps.googleusercontent.com"
GOOGLE_CALENDAR_CLIENT_SECRET="mock_google_client_secret"

# App Environment
NEXT_PUBLIC_APP_URL="http://localhost:3000"
